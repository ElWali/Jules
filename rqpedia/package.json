{
  "name": "rqpedia-allinone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"node server/index.js\" \"cd web && npm run dev\"",
    "build:web": "cd web && npm run build",
    "start": "node server/index.js",
    "build": "npm run build:web",
    "electron": "ELECTRON_SERVE=1 electron .",
    "start:electron": "electron ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "concurrently": "^8.2.0"
  },
  "devDependencies": {
    "electron": "^26.0.0"
  }
}